[package]
name = "solana"
version = "0.1.0"
description = "zKube game program for Solana and MagicBlock"
edition = "2021"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "lib"]
name = "solana"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []
sbf-tests = []

[dependencies]
anchor-lang = { version = "=1.0.2", features = ["init-if-needed"] }
# 0.16.2 is the security floor: earlier `#[ephemeral]` expansions left the
# undelegation buffer and System program callback accounts unconstrained.
ephemeral-rollups-sdk = { version = "=0.16.2", features = ["anchor", "vrf"] }
session-keys = { version = "3.1.1", features = ["no-entrypoint"] }
solana-sha256-hasher = { version = "3.1.0", features = ["sha2"] }
zkube-core = { path = "../../crates/zkube-core", default-features = false }

[dev-dependencies]
base64 = "=0.22.1"
mollusk-svm = "=0.7.0"
serde_json = "1"
solana-account = "=3.0.0"
solana-bpf-loader-program = "=3.0.14"
solana-hash = "=4.3.0"
solana-loader-v3-interface = { version = "=6.1.1", features = ["bincode"] }
solana-message = { version = "=3.1.0", features = ["bincode"] }
zkube-core-host = { path = "../../crates/zkube-core-host" }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
