// Stub — useActiveStoryAttempt (migration Solana)
export interface ActiveStoryRun {
  gameId: bigint;
  zoneId: number;
  level: number;
}

export const useActiveStoryAttempt = (): ActiveStoryRun | null => {
  return null;
};
