// Stub — useCurrentChallenge (migration Solana)
export function useCurrentChallenge() {
  return {
    challenge: undefined as any,
    isLoading: false,
    challengeCount: 0,
  };
}

export default useCurrentChallenge;
