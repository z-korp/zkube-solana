// Stub — useActiveDailyAttempt (migration Solana)
export interface ActiveDailyRun {
  gameId: bigint;
  challengeId: number;
  level: number;
  isReplay: boolean;
}

export const useActiveDailyAttempt = (): ActiveDailyRun | null => {
  return null;
};
