// Stub — usePlayerEntry (migration Solana)
export function usePlayerEntry(_challengeId?: number, _playerAddress?: string) {
  return {
    entry: null as any,
    isRegistered: false,
  };
}

export default usePlayerEntry;
