// Stub — useGame (migration Solana)
export const useGame = (_params: { gameId: bigint | undefined; shouldLog: boolean }) => {
  return {
    game: undefined as any,
    gameKey: undefined as any,
    seed: undefined as bigint | undefined,
  };
};
