// Stub — useSettings (migration Solana)
export function useSettings(_settingsId: number = 1): {
  settings: any;
  isLoading: boolean;
} {
  return { settings: undefined, isLoading: false };
}

export default useSettings;
