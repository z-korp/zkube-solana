// Stub — API Ekubo (DEX Starknet) supprimée lors de la migration Solana
// Ce fichier existe uniquement pour éviter les erreurs d'import dans les hooks legacy.

export async function getSwapQuote(_params?: any): Promise<null> {
  return null;
}
