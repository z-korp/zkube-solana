// Stub — Controller Starknet (migration Solana)
// Ce composant affichait l'adresse/username Cartridge. Plus nécessaire en mode Solana.
export const Controller = ({ className = "" }: { className?: string }) => {
  return null;
};

export default Controller;
